/*
 * TASK #5
 * Think about that, which elements or functions of language could facilitate adding
 * new users whithout a need of reappearing password verification program's compilation.
 */

#include <iostream>

using namespace std;

int main() {

    cout << "Program can run in infinite loop." << endl;
    cin.ignore();
    cout << "Program can end by pressing exit command from menu." << endl;
    cin.ignore();
    cout << "User data can be stored in some array type structure (dictionary etc)." << endl;
    cin.ignore();
    cout << "The data base can be stored in separate file to save information after exiting program." << endl;
    cin.ignore();
    cout << "No more sins to remember." << endl;
    
    return 0;

}